using System;
using System.Collections.Generic;
using System.Text;
using System.Linq;

namespace ImplementingQueue
{
    class CustomQueue<T>
    {
        private QueueElement<T> first;
        private int count;
        public int Count { get { return count; } }

        public void Enqueue(T value)
        {
            HashSet<QueueElement<T>> sfa = new HashSet<QueueElement<T>>();
            sfa.Add
            foreach (var item in sfa)
            {
                item.
            }
        }
    }
}
